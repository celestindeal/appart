import 'package:flutter_test/flutter_test.dart';

/// Test de base pour vérifier que l'app se construit sans erreur.
/// Les vrais tests seront ajoutés au fur et à mesure du développement.
void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // Placeholder : l'app nécessite des providers et du réseau pour démarrer.
    expect(true, isTrue);
  });
}
